# Development tools

This directory contains local tools used while developing and reviewing the Textual interface.

These scripts are **not part of the application** and are not required to run GLACIS.

Keep reusable test coverage under `tests/`. Keep one-off visual experiments here only when they are still useful for maintaining the current UI.
